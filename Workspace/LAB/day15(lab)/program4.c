#include<stdio.h>
#include<string.h>

int main()
{
    char s[50],i;
    printf("input the string:");
    gets(s);
    printf("the characters of the string are:");
    for (i=0;i<s[i];i++)
    {
        printf("%c ",s[i]);
    }

}